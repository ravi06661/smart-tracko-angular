import { ChapterExamResult } from './chapter-exam-result';

describe('ChapterExamResult', () => {
  it('should create an instance', () => {
    expect(new ChapterExamResult()).toBeTruthy();
  });
});
