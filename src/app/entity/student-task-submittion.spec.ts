import { StudentTaskSubmittion } from './student-task-submittion';

describe('StudentTaskSubmittion', () => {
  it('should create an instance', () => {
    expect(new StudentTaskSubmittion()).toBeTruthy();
  });
});
