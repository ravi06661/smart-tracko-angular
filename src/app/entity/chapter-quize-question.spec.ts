import { ChapterQuizeQuestion } from './chapter-quize-question';

describe('ChapterQuizeQuestion', () => {
  it('should create an instance', () => {
    expect(new ChapterQuizeQuestion()).toBeTruthy();
  });
});
