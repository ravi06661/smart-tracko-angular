import { AssignmentSubmission } from './assignment-submission';

describe('AssignmentSubmission', () => {
  it('should create an instance', () => {
    expect(new AssignmentSubmission()).toBeTruthy();
  });
});
