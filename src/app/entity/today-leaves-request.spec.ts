import { TodayLeavesRequest } from './today-leaves-request';

describe('TodayLeavesRequest', () => {
  it('should create an instance', () => {
    expect(new TodayLeavesRequest()).toBeTruthy();
  });
});
