import { SubmissionAssignmentTaskStatus } from './submission-assignment-task-status';

describe('SubmissionAssignmentTaskStatus', () => {
  it('should create an instance', () => {
    expect(new SubmissionAssignmentTaskStatus()).toBeTruthy();
  });
});
