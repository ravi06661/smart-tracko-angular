import { Discussionformsocketresponse } from './discussionformsocketresponse';

describe('Discussionformsocketresponse', () => {
  it('should create an instance', () => {
    expect(new Discussionformsocketresponse()).toBeTruthy();
  });
});
