import { AssignmentSubmissionRequest } from './assignment-submission-request';

describe('AssignmentSubmissionRequest', () => {
  it('should create an instance', () => {
    expect(new AssignmentSubmissionRequest()).toBeTruthy();
  });
});
