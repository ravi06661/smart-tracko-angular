import { AssignmentQuestionRequest } from './assignment-question-request';

describe('AssignmentQuestionRequest', () => {
  it('should create an instance', () => {
    expect(new AssignmentQuestionRequest()).toBeTruthy();
  });
});
