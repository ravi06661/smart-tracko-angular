import { ChapterExamResultResponse } from './chapter-exam-result-response';

describe('ChapterExamResultResponse', () => {
  it('should create an instance', () => {
    expect(new ChapterExamResultResponse()).toBeTruthy();
  });
});
