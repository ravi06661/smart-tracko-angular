import { DiscussionFormResponse } from './discussion-form-response';

describe('DiscussionFormResponse', () => {
  it('should create an instance', () => {
    expect(new DiscussionFormResponse()).toBeTruthy();
  });
});
