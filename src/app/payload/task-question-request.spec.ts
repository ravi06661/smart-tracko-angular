import { TaskQuestionRequest } from './task-question-request';

describe('TaskQuestionRequest', () => {
  it('should create an instance', () => {
    expect(new TaskQuestionRequest()).toBeTruthy();
  });
});
