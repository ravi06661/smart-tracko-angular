import { ChapterContentResponse } from './chapter-content-response';

describe('ChapterContentResponse', () => {
  it('should create an instance', () => {
    expect(new ChapterContentResponse()).toBeTruthy();
  });
});
