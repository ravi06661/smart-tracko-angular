import { CommentResponseForm } from './comment-response-form';

describe('CommentResponseForm', () => {
  it('should create an instance', () => {
    expect(new CommentResponseForm()).toBeTruthy();
  });
});
