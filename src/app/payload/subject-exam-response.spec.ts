import { SubjectExamResponse } from './subject-exam-response';

describe('SubjectExamResponse', () => {
  it('should create an instance', () => {
    expect(new SubjectExamResponse()).toBeTruthy();
  });
});
