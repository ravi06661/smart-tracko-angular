import { AnnouncementRequest } from './announcement-request';

describe('AnnouncementRequest', () => {
  it('should create an instance', () => {
    expect(new AnnouncementRequest()).toBeTruthy();
  });
});
