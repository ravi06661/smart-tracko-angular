import { TechnologyStackResponse } from './technology-stack-response';

describe('TechnologyStackResponse', () => {
  it('should create an instance', () => {
    expect(new TechnologyStackResponse()).toBeTruthy();
  });
});
