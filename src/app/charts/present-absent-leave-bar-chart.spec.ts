import { PresentAbsentLeaveBarChart } from './present-absent-leave-bar-chart';

describe('PresentAbsentLeaveBarChart', () => {
  it('should create an instance', () => {
    expect(new PresentAbsentLeaveBarChart()).toBeTruthy();
  });
});
