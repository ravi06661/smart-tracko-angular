import { MonthWiseAttendanceDTO } from './month-wise-attendance-dto';

describe('MonthWiseAttendanceDTO', () => {
  it('should create an instance', () => {
    expect(new MonthWiseAttendanceDTO()).toBeTruthy();
  });
});
